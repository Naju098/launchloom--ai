/**
 * Hero3DScene is no longer needed — the DribbbleAnimation
 * component is a self-contained animated dashboard preview.
 * This file is kept as a pass-through to avoid import errors.
 */
export default function Hero3DScene() {
  return null;
}
